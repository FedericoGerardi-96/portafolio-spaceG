---
// HeroSection.astro
---

<section
  id="hero"
  class="relative flex min-h-screen flex-col items-center justify-center bg-background/40 px-6 text-center backdrop-blur-[2px]"
>
  {/* Efecto de resplandor (Glow) */}
  <div class="pointer-events-none absolute inset-0 flex items-center justify-center">
    <div class="h-[500px] w-[500px] rounded-full bg-primary/5 blur-[120px]"></div>
  </div>

  <div class="relative z-10 max-w-4xl">
    {/* Tag de Disponibilidad */}
    <div
      data-animate
      class="mb-8 inline-flex items-center gap-2 rounded-full border border-primary/20 bg-primary/5 px-4 py-1.5 opacity-0"
      style="animation-delay: 0.2s"
    >
      <span class="h-1.5 w-1.5 animate-glow-pulse rounded-full bg-primary"></span>
      <span class="font-mono text-xs text-primary">
        Disponible para proyectos
      </span>
    </div>

    {/* Título Principal */}
    <h1
      data-animate
      class="mb-6 text-balance text-4xl font-bold leading-tight tracking-tight text-foreground opacity-0 sm:text-5xl md:text-7xl"
      style="animation-delay: 0.4s"
    >
      Transformo ideas en{" "}
      <span class="bg-gradient-to-r from-primary to-accent bg-clip-text text-transparent">
        experiencias
      </span>{" "}
      digitales
    </h1>

    {/* Subtítulo */}
    <p
      data-animate
      class="mx-auto mb-10 max-w-2xl text-pretty text-base leading-relaxed text-muted-foreground opacity-0 sm:text-lg"
      style="animation-delay: 0.6s"
    >
      Soy <b>Federico Gerardi</b>, desarrollador Front End Ssr con 4 años de experiencia especializado en <b>React</b> y <b>Next.js</b>. 
      Construyo soluciones innovadoras y efectivas, combinando una mentalidad creativa con ingeniería robusta para resolver problemas complejos.
    </p>

    {/* Botones de Acción (CTAs) */}
    <div
      data-animate
      class="flex flex-col items-center gap-4 opacity-0 sm:flex-row sm:justify-center"
      style="animation-delay: 0.8s"
    >
      <a
        href="#projects"
        class="group inline-flex items-center gap-2 rounded-lg bg-primary px-7 py-3 text-sm font-semibold text-primary-foreground transition-all duration-300 hover:shadow-lg hover:shadow-primary/25"
      >
        Ver Proyectos
        <svg class="h-4 w-4 transition-transform group-hover:translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
        </svg>
      </a>
      <a
        href="#about"
        class="inline-flex items-center gap-2 rounded-lg border border-border px-7 py-3 text-sm font-medium text-foreground transition-all duration-300 hover:border-primary/40 hover:bg-secondary"
      >
        Conoce más
      </a>
    </div>
  </div>

  {/* Indicador de Scroll */}
  <div class="absolute bottom-10 left-1/2 -translate-x-1/2">
    <div class="flex flex-col items-center gap-2">
      <span class="text-xs text-muted-foreground">Scroll</span>
      <div class="flex h-8 w-5 items-start justify-center rounded-full border border-muted-foreground/30 p-1">
        <div class="h-1.5 w-1 animate-bounce rounded-full bg-primary"></div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Animación de pulso para el indicador de disponibilidad */
  @keyframes glow-pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(1.2); }
  }
  .animate-glow-pulse {
    animation: glow-pulse 2s infinite;
  }
</style>